# 🔒 ASI Enterprise Secrets Configuration
# Do not commit real secrets. Copy this file to `.env` and fill real values.

GOOGLE_CLIENT_ID=your-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret-here
DATABASE_URL=asi_vault.db
ASI_JWT_SECRET=replace-with-strong-random-secret

# Tachyon bridge (Rust extension)
TACHYON_CORE_LIBRARY_PATH=./tachyon-core/target/release/libtachyon_core.so

# Billing / integration webhooks
PAYMENT_WEBHOOK_SECRET=replace-with-strong-webhook-secret
LINE_CHANNEL_SECRET=replace-with-line-channel-secret
TIKTOK_WEBHOOK_SECRET=replace-with-tiktok-webhook-secret
